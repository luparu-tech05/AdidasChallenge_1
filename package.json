{
  "name": "adidaschallenge",
  "version": "1.0.0",
  "private": true,
  "description": "Simple full-stack solution for the store inventory synchronization challenge.",
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace server",
    "reset": "npm run reset --workspace server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "keywords": [],
  "author": "Lucero Patricia Rueda Galvan",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
